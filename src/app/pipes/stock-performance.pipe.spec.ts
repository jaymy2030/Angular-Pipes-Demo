import { StockPerformancePipe } from './stock-performance.pipe';

describe('StockPerformancePipe', () => {
  it('create an instance', () => {
    const pipe = new StockPerformancePipe();
    expect(pipe).toBeTruthy();
  });
});
